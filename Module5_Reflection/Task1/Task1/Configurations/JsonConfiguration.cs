using Provider.Plugins;
using System.Reflection;


namespace Task1.Configurations
{
    public class JsonConfiguration : Configuration
    {
        //const string providerType = "Json_Configuration_Provider";
        //public override void LoadAgain(ConfigurationItemAttribute configurationAttribute, ProviderManager manager, PropertyInfo property)
        //{
        //    if (manager != null && configurationAttribute.ProviderType == providerType)
        //        manager.LoadSettings(property);
        //}

        //public override void SaveAgain<T>(ConfigurationItemAttribute configurationAttribute, ProviderManager manager, PropertyInfo property, T value)
        //{
        //    if (manager != null && configurationAttribute.ProviderType == providerType)
        //        manager.SaveSettings(property, value);
        //}
    }
}
